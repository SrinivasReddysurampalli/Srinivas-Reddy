#include<stdio.h>
int main()
{
	int num;
	printf("enter the number");
	scanf("%d",&num);
	
     if(num>0)
     {
     	printf("positive");
	 }
	 if(num<0)
	 {
	 	printf("negative");
	 }
	 if(num==0)
	 {
	 	printf("equal");
	 }
	return 0;
}
